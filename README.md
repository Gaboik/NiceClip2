NiceClip is a convenient, 'no bs' clipboard monitor for Windows.

This is the root of the project, for documentation about NiceClip2 specifically, see the README file inside the `./NiceClip2` directory
